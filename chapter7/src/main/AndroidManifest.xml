<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.ewriter.chapter7">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewAnim.ViewAnimActivity" />
        <activity android:name=".animator.TimerTest" />
        <activity android:name=".animator.PropertyTest" />
        <activity android:name=".animator.DropTestActivity" />
        <activity android:name=".vector.VectorActivity"></activity>
    </application>

</manifest>